match(!this.data)(function() {

    this.data = {
        bundleName: 'app',
        pageTitle: 'Ученики',
        route: this.ctx.route
    };

    return applyCtx({
        block : 'page'
    });

});
